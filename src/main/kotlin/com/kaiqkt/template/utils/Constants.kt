package ${package}.utils

import org.slf4j.MDC
import java.util.UUID

object Constants {
    object Parameters {
        const val REQUEST_ID = "requestId"
    }

    object Keys

    object Headers

    object Sort
}
