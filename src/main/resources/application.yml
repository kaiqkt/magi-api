---
server:
  port: 8080

spring:
  application:
    name: magi-api
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
      base-path: /
  metrics:
    tags:
      application: magi-api
